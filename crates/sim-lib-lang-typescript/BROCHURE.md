# TypeScript notation, without a second runtime

Load erasable TypeScript through `codec/typescript`, inspect faithful annotations
as non-enforcing Shape metadata, and run the resulting graph through exactly the
same evaluator and effects as JavaScript. Compiler-dependent types and
code-producing syntax fail closed and remain located gaps.
