[package]
name = "sim-lib-study"
version = "0.2.0"
edition.workspace = true
license.workspace = true
readme = "README.md"
description = "Domain-neutral durable study lifecycle over sealed coordinates."
repository.workspace = true
homepage.workspace = true
publish = true

[package.metadata.sim-recipes]
policy = "book"

[dependencies]
sha2 = "0.10"
sim-kernel = { version = "0.4.0" }
sim-lib-journal = { version = "0.2.0" }
sim-study-core = { version = "0.2.0" }
thiserror = "2"

[lints]
workspace = true
