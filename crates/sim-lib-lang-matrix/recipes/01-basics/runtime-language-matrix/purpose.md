# Runtime language matrix (descriptor)

This documents the assembled runtime language-conformance matrix across every language codec
profile -- an aggregate test-matrix descriptor rows-by-features, verified under `cargo test`,
not a runnable sandbox expression.
