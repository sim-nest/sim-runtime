# sim-lib-incremental

Load the incremental query organ when a SIM runtime surface needs reusable
memoized calculation over expression values. The organ provides explicit
registration, invalidation, verification, explanation, snapshot, and metrics
functions guarded by small capabilities.
