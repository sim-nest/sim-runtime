# Incremental Query Family

This descriptor shows the Lisp-facing incremental organ surface: create an
engine, register a query family, record a missing dependency, invalidate that
missing key, verify again, and inspect metrics that expose cutoff.
