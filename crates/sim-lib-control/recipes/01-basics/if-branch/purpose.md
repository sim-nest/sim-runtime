# Take the true branch

`if` is a control organ: a special form that evaluates its test and then only
the taken branch, so the untaken branch never runs. With a true test this
returns the then-branch `10`; the else-branch `20` is left unevaluated.
