# Direct JavaScript source

This recipe enters solely through `codec/javascript`, selects
`lang/javascript-core/v1`, and directly evaluates the stable lowering. It does
not compile, emit instructions, invoke Node, or delegate to a foreign engine.
