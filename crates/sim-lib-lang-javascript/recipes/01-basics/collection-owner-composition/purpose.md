# JavaScript policy over the sequence organ

This specimen keeps holes, SameValueZero key policy, and JavaScript iterator
result shapes at the guest boundary while routing sparse storage and live
insertion-order traversal through `sim-lib-sequence`. It owns no sparse store or
ordered table.
