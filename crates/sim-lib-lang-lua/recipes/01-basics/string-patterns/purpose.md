# Lua string pattern case

This records the Lua source case where a pattern replacement returns both the
changed string and the replacement count. The runtime conformance test checks
that the Lua profile uses the shared text-pattern machinery.
