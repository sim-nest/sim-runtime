id = "select-overload"
title = "Select an overload"
codec = "lisp"
setup = "setup.siml"
purpose = "purpose.md"
order = 10
tags = ["dispatch", "overload", "descriptor", "sandbox-descriptor"]
requires = ["core", "codec/lisp"]
